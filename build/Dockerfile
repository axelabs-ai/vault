# Placeholder — actual build is orchestrated by build/build.sh,
# which clones Timshel at TIMSHEL_REF, applies patches/*.patch, then
# invokes `docker build` against the patched Timshel source tree using
# upstream Timshel's own Dockerfile.
#
# We avoid duplicating Timshel's multi-stage Rust build here to minimize
# the maintenance diff when tracking upstream releases.
#
# See: build/build.sh
